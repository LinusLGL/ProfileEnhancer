# Streamlit Secrets Template
# Copy this to secrets.toml and add your actual API key
# For Streamlit Community Cloud: Add this configuration in your app's secrets section

[openai]
api_key = "your_openai_api_key_here"

# How to set up for Streamlit Community Cloud:
# 1. Deploy your app on Streamlit Community Cloud
# 2. Go to your app's settings/secrets  
# 3. Add your OpenAI API key in the format above
# 4. Save the secrets configuration
# 5. Your app will restart automatically with the new configuration

# Note: The backend has a fallback API key configured for seamless operation